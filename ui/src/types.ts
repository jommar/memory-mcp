import type { ConsolidateReport } from '../../src/core/lifecycle.js';
import type { DetailPayload, GraphPayload, StatsPayload } from '../server/api.js';

export type { ConsolidateReport, DetailPayload, GraphPayload, StatsPayload };
export type {
  GraphEdge,
  GraphNode,
  NeighborSummary,
} from '../server/api.js';
